import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    icon: {
        fontSize: 20,
        paddingHorizontal: 20,
    },
    image: {
        alignItems: 'center',
        flexDirection: 'row',
    },
});

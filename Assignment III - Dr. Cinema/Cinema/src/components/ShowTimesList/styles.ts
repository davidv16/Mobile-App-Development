import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: 300,
    paddingBottom: 10,
  },
  boardTitle: {
    width: '100%',
    textAlign: 'center',
    fontSize: 15,
  },
});

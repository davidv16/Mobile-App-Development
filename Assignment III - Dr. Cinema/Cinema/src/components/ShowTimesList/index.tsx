import React, { useEffect } from 'react';
import { View, Text } from 'react-native';
import styles from './styles';
import { getMovies } from '../../services';

const ShowTimesList = () => {
  return (
    <View>
      <Text>ShowTimesList</Text>
    </View>
  );
}

export default ShowTimesList;